<aside class="col-md-4">
  <section class="user_info">
    <h1>
      <% if @user.avatar? %>
          <%= image_tag @user.avatar.medium.url %>
        <% else %>
          <%= gravatar_for @user %>
        <% end %>
      <%= @user.name %>
    </h1>
  </section>
</aside>