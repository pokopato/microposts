<%= form_for(current_user.favoriteships.find_by(micropost_id: @feed_item.id),
             html: { method: :delete }, remote: true) do |f| %>
  <div><%= hidden_field_tag :micropost_id, @feed_item.id %></div>
  <%= f.submit "お気に入りから削除", class: "btn" %>
<% end %>